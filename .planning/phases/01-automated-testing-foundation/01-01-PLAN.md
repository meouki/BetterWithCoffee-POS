# Plan: Automated Testing Foundation (Phase 1)
**Wave**: 01
**Depends on**: None
**Files Modified**: backend_rewrite/package.json, frontend/package.json, backend_rewrite/server.js, frontend/vite.config.js, backend_rewrite/app.js (new)
**Autonomous**: true

## Goal
Establish a robust testing foundation using Vitest for both frontend and backend, with specific coverage for transaction calculations and inventory deduction.

## Tasks

### 1-01: Backend Environment Setup [BLOCKING]
- **Action**: 
    1. Install dependencies in `backend_rewrite/`: `vitest`, `supertest`.
    2. Refactor `backend_rewrite/server.js` to extract the Express application logic into a new file `backend_rewrite/app.js`.
    3. Export `app` from `app.js` and `require` it in `server.js` for the `listen` call.
    4. Create `backend_rewrite/vitest.config.js` with Node environment settings.
- **Read First**: `backend_rewrite/server.js`, `backend_rewrite/package.json`.
- **Acceptance Criteria**:
    - `backend_rewrite/app.js` exists and exports the `app` instance.
    - `backend_rewrite/server.js` still starts the server correctly on port 5000.
    - `backend_rewrite/package.json` contains `vitest` in `devDependencies`.

### 1-02: Frontend Environment Setup
- **Action**:
    1. Install dependencies in `frontend/`: `vitest`, `@testing-library/react`, `jsdom`, `@testing-library/jest-dom`.
    2. Update `frontend/vite.config.js` to include the `test` configuration (environment: `jsdom`).
    3. Create `frontend/src/test/setup.js` importing `@testing-library/jest-dom`.
- **Read First**: `frontend/vite.config.js`, `frontend/package.json`.
- **Acceptance Criteria**:
    - `frontend/vite.config.js` has a `test` property with `globals: true` and `environment: 'jsdom'`.
    - `frontend/package.json` contains `vitest` in `devDependencies`.

### 1-03: Transaction Logic Tests (Backend) [BLOCKING]
- **Action**:
    1. Create `backend_rewrite/tests/transactions.test.js`.
    2. Implement tests for order price calculations:
        - Base product price * quantity.
        - Product size variation pricing.
        - Modifier additional costs.
- **Read First**: `backend_rewrite/routes/orders.js`, `backend_rewrite/models/order.js` (if exists).
- **Acceptance Criteria**:
    - `backend_rewrite/tests/transactions.test.js` exists.
    - Tests verify totals with complex combinations (e.g., 2 Lattes + 1 Sugar addon).
    - Tests pass without side-effects on the production database.

### 1-04: Sales & Inventory Tests (Backend)
- **Action**:
    1. Create `backend_rewrite/tests/inventory.test.js`.
    2. Implement tests for stock deduction:
        - Creating an order reduces the `Inventory` count for relevant ingredients.
        - A `StockLog` entry is created for each deduction with `reason: 'sale'`.
- **Read First**: `backend_rewrite/routes/inventory.js`, `backend_rewrite/models/inventory.js`.
- **Acceptance Criteria**:
    - `backend_rewrite/tests/inventory.test.js` exists.
    - Assertions confirm matching stock counts before/after simulated sales.
    - `StockLog` verification passes.

## Verification
- [ ] Run `npm test` in `backend_rewrite/` — all tests pass.
- [ ] Run `npm test` in `frontend/` — runner starts correctly (even if no tests yet).
- [ ] Verify `backend_rewrite/server.js` can still be started manually with `node server.js`.
